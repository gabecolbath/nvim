local opts = {
    ui = {
        border = "single",
        icons = {
            package_installed = "",
            package_pending = "", 
            package_uninstalled = "",
        },
    },
}

return {
    opts = opts, 
}
